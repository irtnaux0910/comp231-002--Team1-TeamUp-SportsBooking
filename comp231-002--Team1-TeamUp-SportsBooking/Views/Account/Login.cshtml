@model comp231_002__Team1_TeamUp_SportsBooking.Models.LoginViewModel

@{
    ViewData["Title"] = "Sign In";
}

<section class="auth-page">
    <div class="auth-card auth-card-grid">

        <!-- LEFT -->
        <div class="auth-card-left">
            <h1 class="h3 fw-semibold mb-3">
                Welcome back 👋
            </h1>

            <p class="text-soft mb-4">
                Log in to book courts, join games, and connect with players.
            </p>

            <form asp-controller="Account" asp-action="Login" method="post" class="mt-3">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>  <!-- Added for error summary -->

                <div class="mb-3">
                    <label asp-for="Email" class="form-label"></label>
                    <input asp-for="Email" class="form-control" placeholder="you@example.com" />
                    <span asp-validation-for="Email" class="text-danger"></span>  <!-- Added for field validation -->
                </div>

                <div class="mb-3">
                    <label asp-for="Password" class="form-label"></label>
                    <input asp-for="Password" class="form-control" placeholder="••••••••" />
                    <span asp-validation-for="Password" class="text-danger"></span>  <!-- Added for field validation -->
                </div>

                <div class="d-flex justify-content-between align-items-center mb-4">
                    <div class="form-check">
                        <input asp-for="RememberMe" class="form-check-input" />
                        <label asp-for="RememberMe" class="form-check-label text-soft small"></label>
                    </div>

                    <a class="text-soft small" asp-action="ForgotPassword">Forgot password?</a>
                </div>

                <button type="submit" class="btn-primary-teamup w-100">
                    Sign In
                </button>
            </form>

            <p class="mt-4 small text-soft">
                Don’t have an account?
                <a asp-action="Register" class="text-accent">Create one</a>
            </p>
        </div>

        <!-- RIGHT -->
        <div class="auth-card-right">
            <h2 class="h5 fw-semibold mb-3">Tonight’s activity</h2>

            <div class="d-flex flex-column gap-3 small">
                <div class="card-teamup">
                    <div class="d-flex justify-content-between">
                        <span>Downtown Court A</span>
                        <span class="badge-green">Booked</span>
                    </div>
                    <p class="text-soft mt-1 mb-0">19:00 · Tennis</p>
                </div>

                <div class="card-teamup">
                    <div class="d-flex justify-content-between">
                        <span>Park Field 2</span>
                        <span class="badge-green">2 spots left</span>
                    </div>
                    <p class="text-soft mt-1 mb-0">21:30 · Soccer</p>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />  <!-- For client-side validation -->
}